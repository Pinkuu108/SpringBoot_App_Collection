package Arrayes;

public class AllQuestion {
	/*
	 * 🔹Advanced/ Logical Array Questions
	 * 
	 * Find the largest
	 * 
	 * sum subarray (Kadane’s Algorithm).
	 * 
	 * Move all zeroes to the end of the array.
	 * 
	 * Check whether the array is sorted or not.
	 * 
	 * Find pairs with a given sum.
	 * 
	 * Find duplicate elements in an array.
	 * 
	 * Find the intersection of two arrays.
	 * 
	 * Find the union of two arrays.
	 * 
	 * Rearrange array elements in alternate positive and negative order.
	 * 
	 * Find the majority element in an array.
	 * 
	 * Find the kth largest and kth smallest element.
	 */
}
